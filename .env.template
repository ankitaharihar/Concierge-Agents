# Copy this file to `.env` and fill in the values. Do NOT commit the filled `.env`.

GOOGLE_API_KEY=your_google_api_key_here
# Recommended model choices (pick one based on cost/latency/quality):
# - `models/gemini-2.5-pro` : Best quality / higher cost (recommended for assistant/dialog and complex tasks)
# - `models/gemini-2.5-flash`: Lower latency and cost, good for interactive use where speed matters
# - `models/gemini-1.0-lite`  : Minimal cost for simple completions or testing
# Choose the model resource string you prefer (do NOT commit your `.env`).
# Default below is set to a high-quality model; change if you prefer lower cost/latency.
MODEL_NAME=models/gemini-2.5-pro
